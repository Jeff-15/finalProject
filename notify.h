#include <bits/stdc++.h>
#include "subject.h"
#include "board.h"
#include "gameBoard.h"

class notify {
    GameBoard* gb;

  public:
    void printBoard(std::vector <Tile*> tiles, std::vector <Vertex*> vertices, std::vector <Edge*> edges);
    
};
